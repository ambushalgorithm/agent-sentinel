{
  "name": "victim-agent",
  "version": "1.0.0",
  "description": "Simulates victim agent behavior",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest",
    "test:unit": "jest --testPathPattern='test/unit'",
    "test:e2e": "jest --testPathPattern='test/e2e'",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
